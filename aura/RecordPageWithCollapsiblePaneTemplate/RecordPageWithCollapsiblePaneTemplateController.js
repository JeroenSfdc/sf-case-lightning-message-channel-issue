({
    handleExpand: function(component, message, helper) {
        debugger;
/*        if (message != null && message.getParam("expanded") != null) {
            const expanded = message.getParam("expanded");
    //        let collapsed;
console.log('The value of expanded = ' + expanded);
console.log('The value of expanded.value = ' + expanded.value);

//            expanded.toUpperCase() === "TRUE" ? collapsed = true : collapsed = false;

  //          component.set("v.isSidebarCollapsed", collapsed);
        }*/
    }
})